const player = {
    name: 'Messi',
    club: 'Inter Miami',
    salary: 500000
}

// const name = player.name;
// const salary = player.salary;

const {name, salary} = player;
console.log(name, salary);